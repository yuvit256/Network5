# import scapy.all as scapy

# ip = scapy.IP(dst="1.2.3.4")
# udp = scapy.UDP(sport=64123, dport=12345)  # 65535
# raw = scapy.Raw(load="hello world")

# pkt = ip / udp / raw

# scapy.send(pkt)

list = [i for i in range(0, 10)]

